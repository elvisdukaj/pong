export namespace vis::ecs {
using vis::ecs::basic_entt_traits;
using vis::ecs::to_entity;
using vis::ecs::to_integral;
using vis::ecs::to_version;

using vis::ecs::null_t;
using vis::ecs::tombstone_t;

using vis::ecs::null;
using vis::ecs::tombstone;
} // namespace vis::ecs